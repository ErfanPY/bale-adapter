# runtime deps (installed by scripts/bootstrap.sh on the VPS)
# aiobale is bundled in vendor/aiobale-source.zip (PyPI is an empty shell;
# Enalite/aiobale GitHub repo was taken down; mirrors don't ship a setup.py).
git+https://github.com/mehrad1232/Aiobale.git
pydantic
aiohttp
aiofiles<24
httpx
pyyaml
blackboxprotobuf
colorama
magic-filter
